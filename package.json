{
  "name": "worldmap-topojson",
  "version": "1.0.0",
  "description": "A worldmap with colored country accorded to specific rules. using leaflet, topoJson",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "topojson": "~3.0.2"
  }
}
